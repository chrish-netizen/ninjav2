version: '3.8'

services:
  ninja-bot:
    build: .
    container_name: ninjav2-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Mount storage directory to persist all data
      - ./storage:/app/storage
